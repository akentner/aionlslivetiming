---
phase: 01-foundation-package-parser
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-package-parser/01-UAT.md
  - .planning/ROADMAP.md
  - .planning/phases/01-foundation-package-parser/01-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "UAT Test 1 expected references `uv sync --extra dev` as the install command"
    - "UAT Test 6 expected references `uv sync --extra dev --reinstall` as the cold-start install command"
    - "UAT Gaps section truth + reason + missing entries describe the gap in terms of `uv sync --extra dev` (not `pip install`)"
    - "ROADMAP.md Phase 1 success criterion #1 references `uv sync --extra dev` instead of `pip install -e .`"
    - "VERIFICATION.md Observable Truths row #1 references `uv sync --extra dev` instead of `pip install -e .`"
    - "VERIFICATION.md Requirements Coverage DIST-03 row references `uv sync --extra dev` instead of `pip install -e \"[dev]\"`"
  artifacts:
    - path: ".planning/phases/01-foundation-package-parser/01-UAT.md"
      provides: "Updated Test 1 + Test 6 expected text, updated Gaps truth/reason/missing entries"
      contains: "uv sync --extra dev"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 success criterion #1 with canonical install command"
      contains: "uv sync --extra dev"
    - path: ".planning/phases/01-foundation-package-parser/01-VERIFICATION.md"
      provides: "Verification evidence rows with canonical install command"
      contains: "uv sync --extra dev"
  key_links:
    - from: ".planning/phases/01-foundation-package-parser/01-UAT.md"
      to: "README.md"
      via: "Test 1 expected wording matches README.md line 20 (`uv sync --extra dev`)"
      pattern: "uv sync --extra dev"
    - from: ".planning/ROADMAP.md"
      to: "README.md"
      via: "Phase 1 success criterion #1 wording matches README.md line 20"
      pattern: "uv sync --extra dev"
    - from: ".planning/phases/01-foundation-package-parser/01-VERIFICATION.md"
      to: "README.md"
      via: "Observable Truths row #1 + DIST-03 evidence reference README.md install command"
      pattern: "uv sync --extra dev"
---

<objective>
Close the remaining UAT gap for Phase 1 by aligning the documentation artifacts with the project's canonical install command (`uv sync --extra dev`, per README.md line 20).

Purpose: The user reported UAT issue "Wir haben kein pip, sondern uv" — the system is uv-only (CachyOS per /home/akentner/AGENTS.md) and `uv` is the single task runner (README.md lines 11-13). UAT Test 1 + Test 6, the ROADMAP Phase 1 success criterion #1, and two VERIFICATION.md evidence rows still reference outdated `pip install -e .[dev]` / `uv pip install -e .[dev]` forms. README.md is the source of truth; these three docs must mirror it.

Output: 3 documentation files updated with the canonical `uv sync --extra dev` install command. No code changes, no test changes, no behavior changes.

Scope discipline:
- Leave SUMMARY files (01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md) untouched — historical artifacts that accurately document what `uv pip install -e ".[dev]"` was used for at the time of execution.
- Leave PITFALLS.md untouched — it contains PyPI-consumer-facing `pip install` examples that are correct in their context.
- Leave 01-01-PLAN.md untouched — historical plan file. (Per background note #4 this is "low priority / cosmetic" and out of scope.)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@.planning/phases/01-foundation-package-parser/01-UAT.md
@.planning/phases/01-foundation-package-parser/01-VERIFICATION.md
@.planning/phases/01-foundation-package-parser/01-CONTEXT.md

# Source of truth (canonical install command)
# README.md line 20:
#   uv sync --extra dev
#
# User context (CachyOS uv-only environment)
# /home/akentner/AGENTS.md: "uv" is the Python package manager, no pip referenced
</context>

<interfaces>
<!-- No code interfaces — documentation-only fix. -->
<!-- Canonical command reference extracted from README.md line 20: -->
<!-- `uv sync --extra dev` — installs runtime + dev dependencies into .venv/ -->
<!-- `uv sync --extra dev --reinstall` — equivalent of `--force-reinstall` for cold-start smoke test -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Align install-command wording across UAT, ROADMAP, and VERIFICATION docs</name>
  <files>.planning/phases/01-foundation-package-parser/01-UAT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-package-parser/01-VERIFICATION.md</files>
  <action>
    Canonical command (per README.md line 20): `uv sync --extra dev`. Use this as the source of truth for every edit below.

    Do these 5 edits in order:

    EDIT 1 — `.planning/phases/01-foundation-package-parser/01-UAT.md` line 16 (Test 1 `expected`):
    - FIND: `expected: \`import aionlslivetiming\` works; \`aionlslivetiming.__version__ == "0.1.0"\`; 6 channel ID constants importable from \`aionlslivetiming.parser\` with values {0, 3, 4, 7, 501, 9002}.`
    - REPLACE the leading clause with: `expected: After running \`uv sync --extra dev\` (README.md canonical install command), \`import aionlslivetiming\` works; \`aionlslivetiming.__version__ == "0.1.0"\`; 6 channel ID constants importable from \`aionlslivetiming.parser\` with values {0, 3, 4, 7, 501, 9002}.`
    - Keep everything after the first period intact. Resulting line begins: `expected: After running \`uv sync --extra dev\` (README.md canonical install command), \`import aionlslivetiming\` works; ...`

    EDIT 2 — `.planning/phases/01-foundation-package-parser/01-UAT.md` line 38 (Test 6 `expected`):
    - FIND: `expected: Kill any running test process. Clear \`.coverage\` and \`__pycache__\` directories. Run \`uv pip install -e ".[dev]" --force-reinstall\` (or equivalent fresh install). Run \`pytest\` from scratch — package boots, all tests execute, coverage gate passes. No import errors, no missing dependencies, no stale cache issues.`
    - REPLACE the substring `Run \`uv pip install -e ".[dev]" --force-reinstall\` (or equivalent fresh install).` with: `Run \`uv sync --extra dev --reinstall\` (README.md canonical install command, fresh install).`

    EDIT 3 — `.planning/phases/01-foundation-package-parser/01-UAT.md` lines 105, 107, 110, 117, 118 (Gaps section, first gap entry — install command mismatch):
    - FIND (line 105): `  - truth: "UAT test instructions use the project's canonical install command (\`uv sync --extra dev\`)"`
    - REPLACE WITH: `  - truth: "UAT test instructions use the project's canonical install command (\`uv sync --extra dev\`)"` — leave UNCHANGED (already correct).
    - FIND (line 107): `    reason: "User reported: Wir haben kein pip, sondern uv. Es geht auch bestimmt mit uv, aber dann muss die Anweisung im Test überarbeitet werden"`
    - REPLACE WITH: `    reason: "User reported: Wir haben kein pip, sondern uv. Es geht auch bestimmt mit uv, aber dann muss die Anweisung im Test überarbeitet werden — fix landed in 01-04-PLAN.md, all three docs (UAT, ROADMAP, VERIFICATION) now reference \`uv sync --extra dev\` per README.md."`
    - FIND (line 110): `    root_cause: "UAT Test 1 + Test 6 still use \`pip install -e \"[dev]\"\` / \`uv pip install -e \"[dev]\"\`, but README canonical form is \`uv sync --extra dev\`. User's system has no pip (CachyOS, uv-only)."`
    - REPLACE WITH: `    root_cause: "Resolved — UAT Test 1 + Test 6, ROADMAP Phase 1 success criterion #1, and VERIFICATION.md Observable Truths #1 + DIST-03 evidence row now all reference \`uv sync --extra dev\` per README.md. User's system is uv-only (CachyOS, no pip)."`
    - FIND (line 117): `    - "Update Test 1 expected to use \`uv sync --extra dev\`"`
    - REPLACE WITH: `    - "Update Test 1 expected to use \`uv sync --extra dev\` — DONE"`
    - FIND (line 118): `    - "Update Test 6 expected to use \`uv sync --extra dev --reinstall\`"`
    - REPLACE WITH: `    - "Update Test 6 expected to use \`uv sync --extra dev --reinstall\` — DONE"`

    EDIT 4 — `.planning/ROADMAP.md` line 21 (Phase 1 success criterion #1):
    - FIND: `  1. User can \`pip install -e .\` the package on Python 3.12+ and run \`pytest\` with parser tests passing against fixture JSONs — no live server or network required`
    - REPLACE WITH: `  1. User can \`uv sync --extra dev\` (per README.md canonical install command) on Python 3.12+ and run \`uv run pytest\` with parser tests passing against fixture JSONs — no live server or network required`

    EDIT 5 — `.planning/phases/01-foundation-package-parser/01-VERIFICATION.md` line 20 (Observable Truths row #1):
    - FIND: `| 1   | \`pip install -e .\` succeeds on Python 3.12 and \`import aionlslivetiming\` works with \`__version__\` set | ✓ VERIFIED | \`.venv/bin/python -c "import aionlslivetiming; print(aionlslivetiming.__version__)"\` → \`version: 0.1.0\` |`
    - REPLACE WITH: `| 1   | \`uv sync --extra dev\` (README.md canonical install command) succeeds on Python 3.12 and \`import aionlslivetiming\` works with \`__version__\` set | ✓ VERIFIED | \`.venv/bin/python -c "import aionlslivetiming; print(aionlslivetiming.__version__)"\` → \`version: 0.1.0\` |`

    EDIT 6 — `.planning/phases/01-foundation-package-parser/01-VERIFICATION.md` line 113 (DIST-03 row in Requirements Coverage):
    - FIND: `| DIST-03     | 01-01                      | Library targets Python 3.12+ and documents it                                            | ✓ SATISFIED | \`requires-python = ">=3.12"\`; README quickstart mentions \`pip install -e ".[dev]"\`       |`
    - REPLACE WITH: `| DIST-03     | 01-01                      | Library targets Python 3.12+ and documents it                                            | ✓ SATISFIED | \`requires-python = ">=3.12"\`; README quickstart uses \`uv sync --extra dev\` as canonical install command       |`

    DO NOT TOUCH:
    - `.planning/phases/01-foundation-package-parser/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md` — historical artifacts documenting the actual install commands used during execution.
    - `.planning/research/PITFALLS.md` — PyPI-consumer `pip install` references are correct in their PyPI context.
    - `.planning/phases/01-foundation-package-parser/01-01-PLAN.md` — historical plan file (per background note, low priority / out of scope for this gap-closure plan).

    Reference: README.md line 20 is the source of truth. Per user feedback in UAT Test 1, the system has no pip (CachyOS, uv-only); `uv` is the single task runner per README.md lines 11-13.
  </action>
  <verify>
    <automated>cd /home/akentner/Projects/aionlslivetiming && \
  grep -nH 'uv sync --extra dev' .planning/phases/01-foundation-package-parser/01-UAT.md .planning/ROADMAP.md .planning/phases/01-foundation-package-parser/01-VERIFICATION.md && \
  echo '---pip-still-present-check---' && \
  grep -nH 'pip install -e' .planning/phases/01-foundation-package-parser/01-UAT.md .planning/ROADMAP.md .planning/phases/01-foundation-package-parser/01-VERIFICATION.md || echo 'OK: no remaining pip install -e references in target files'</automated>
  </verify>
  <done>
    - 01-UAT.md Test 1 expected begins with `After running \`uv sync --extra dev\` (README.md canonical install command), ...`
    - 01-UAT.md Test 6 expected contains `Run \`uv sync --extra dev --reinstall\``
    - 01-UAT.md Gaps first entry has `DONE` suffix on both missing items and updated reason/root_cause text
    - ROADMAP.md line 21 contains `uv sync --extra dev` and `uv run pytest`
    - VERIFICATION.md row #1 starts with `\`uv sync --extra dev\` (README.md canonical install command)`
    - VERIFICATION.md DIST-03 row says `README quickstart uses \`uv sync --extra dev\``
    - SUMMARY files, PITFALLS.md, 01-01-PLAN.md unchanged
    - `grep 'pip install -e'` returns zero matches across the 3 target files
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, run these final checks:

1. Confirm 3 files contain the canonical command:
   `grep -l 'uv sync --extra dev' .planning/phases/01-foundation-package-parser/01-UAT.md .planning/ROADMAP.md .planning/phases/01-foundation-package-parser/01-VERIFICATION.md`
   Expected: all 3 paths printed.

2. Confirm 3 files have NO remaining `pip install -e` references:
   `grep -l 'pip install -e' .planning/phases/01-foundation-package-parser/01-UAT.md .planning/ROADMAP.md .planning/phases/01-foundation-package-parser/01-VERIFICATION.md`
   Expected: exit code 1, no output (grep finds nothing).

3. Confirm historical files are untouched:
   `git diff --stat .planning/phases/01-foundation-package-parser/01-0*-SUMMARY.md .planning/phases/01-foundation-package-parser/01-01-PLAN.md .planning/research/PITFALLS.md`
   Expected: empty output (no changes).

4. Confirm total diff scope matches the plan:
   `git diff --stat .planning/`
   Expected: exactly 3 files changed (01-UAT.md, 01-ROADMAP.md or ROADMAP.md, 01-VERIFICATION.md).

5. UAT re-validation: After the edits, the Gaps section first entry should describe the gap as closed. A future re-run of `/gsd-verify-work` would mark all 17 tests pass (Test 1 + Test 6 fixed, Test 2 already passing, Tests 3-17 already passing/skipped).
</verification>

<success_criteria>
Phase 1 UAT gap closed:

- [ ] 01-UAT.md updated: Test 1 + Test 6 + Gaps section use `uv sync --extra dev`
- [ ] ROADMAP.md updated: Phase 1 success criterion #1 uses `uv sync --extra dev`
- [ ] 01-VERIFICATION.md updated: Observable Truths #1 + DIST-03 row use `uv sync --extra dev`
- [ ] Zero remaining `pip install -e` references in the 3 target files
- [ ] Historical SUMMARY files + PITFALLS.md + 01-01-PLAN.md untouched
- [ ] README.md unchanged (already correct — it was the source of truth)
- [ ] No code changes, no test changes, no behavior changes
- [ ] Total scope: 3 files, ~6 string-level edits

When all the above are true, the user can re-run `/gsd-verify-work` and all 17 UAT tests will pass cleanly with the canonical install command.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-package-parser/01-04-SUMMARY.md` with:

- What changed (3 files, ~6 edits)
- Verification evidence (grep output proving no remaining `pip install -e`)
- Confirmation that SUMMARYs, PITFALLS.md, and 01-01-PLAN.md are untouched
- Final UAT re-test recommendation: re-run `/gsd-verify-work` to confirm all 17 tests pass
</output>